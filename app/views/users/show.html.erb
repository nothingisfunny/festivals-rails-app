<% if current_user.name %>
		Name: <%= current_user.name%></br>
<% end %>

<% if @user == current_user %>
		Email: <%= current_user.email %></br>
		<%= link_to "Edit Profile", edit_user_path(current_user) %><br>
		<%= link_to "Home Page", root_path %> 
		Upcoming Events: 
		<% UserFestival.all.find
		Saved Events:
<% end %>